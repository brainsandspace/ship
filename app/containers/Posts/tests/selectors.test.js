// import { fromJS } from 'immutable';
// import { makeSelectPostsDomain } from '../selectors';

// const selector = makeSelectPostsDomain();

describe('makeSelectPostsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
