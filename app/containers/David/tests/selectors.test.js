// import { fromJS } from 'immutable';
// import { makeSelectDavidDomain } from '../selectors';

// const selector = makeSelectDavidDomain();

describe('makeSelectDavidDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
