// import { fromJS } from 'immutable';
// import { makeSelectMarginalsDomain } from '../selectors';

// const selector = makeSelectMarginalsDomain();

describe('makeSelectMarginalsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
