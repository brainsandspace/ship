// import { fromJS } from 'immutable';
// import { makeSelectInspiratorsDomain } from '../selectors';

// const selector = makeSelectInspiratorsDomain();

describe('makeSelectInspiratorsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
